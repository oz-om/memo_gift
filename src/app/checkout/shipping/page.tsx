import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Shipping & complete order",
  description: "make memories beautiful with amazing gifts",
};

export default function Shipping() {
  return (
    <div>
      <h1>shipping</h1>
    </div>
  );
}
